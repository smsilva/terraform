#!/bin/bash

FILE_NAME="$1"
DIRECTORY=$(dirname "${FILE_NAME}")

echo "FILE_NAME.: ${FILE_NAME}"
echo "DIRECTORY.: ${DIRECTORY}"

mkdir -p ${DIRECTORY?}

if [ -e "${FILE_NAME}" ]; then
  echo "${FILE_NAME}: yes"
  rm -rf "${FILE_NAME}"
  touch "${FILE_NAME}"
else
  echo "${FILE_NAME}: yes"
fi
